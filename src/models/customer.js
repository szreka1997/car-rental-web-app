class Customer {
  constructor({ id, fullName, email, address, phone }) {
    this.id = id;
    this.fullName = fullName;
    this.email = email;
    this.address = address;
    this.phone = phone;
  }
}

export default Customer;
